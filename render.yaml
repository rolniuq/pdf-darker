services:
  - type: web
    name: pdf-darker
    env: node
    region: oregon
    plan: free
    buildCommand: corepack enable && pnpm install --frozen-lockfile && pnpm build:shared && pnpm build:core && pnpm build:ui
    startCommand: node packages/ui/dist/server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 22
